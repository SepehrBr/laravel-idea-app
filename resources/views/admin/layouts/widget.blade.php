<div class="card">
    <div class="card-body">
        <p class="fs-4">
            <i class="{{ $icon ?? 'fas fa-user' }}" width="15"></i>
            {{ $title ?? '' }}
        </p>
        <p class="fw-bold fs-3">{{ $users ?? 0 }}</p>
    </div>
</div>
